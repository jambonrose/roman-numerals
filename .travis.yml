branches:
  only:
    - master
language: python
python:
  - 3.4
  - 3.5
  - 3.6
cache:
  pip: true
install:
  - pip install .
  - pip install codecov
script:
  - py.test --cov=roman_numerals -vv tests
after_success:
  - codecov
